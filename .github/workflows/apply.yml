name: Submit Application to B12

on:
  workflow_dispatch:

jobs:
  submit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run submission script
        env:
          APPLICANT_NAME: "Fredrick Karuri"
          APPLICANT_EMAIL: "fredrickkmaish@gmail.com"
          RESUME_LINK: "https://linkedin.com/in/fredrick-karuri"
          SIGNING_SECRET: ${{ secrets.SIGNING_SECRET }}
        run: python scripts/submit.py